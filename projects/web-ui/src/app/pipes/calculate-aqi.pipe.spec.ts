import { CalculateAqiPipe } from './calculate-aqi.pipe';

describe('CalculateAqiPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateAqiPipe();
    expect(pipe).toBeTruthy();
  });
});
